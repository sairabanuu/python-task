# 1. What are variables and why are they called variables? Assign two numbers to variables `a` and `b`, and print their sum.
# Variables are names to store data values and variable as a container that holds information
# them change the values during the execution of a program.
a = 10
b = 20
print(a+b)
